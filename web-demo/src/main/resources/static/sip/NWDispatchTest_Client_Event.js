
// Copyright 2017-2018 CoCall.Net

//功能：实现 Dispatch 客户端事件操作部分

//应用可根据需要修改内部事件实现

//注意：这里可能需要供VBS调用，VBScript 函数调用javascrip函数时  javascrip函数中的 ；必须全部去掉 不然调用不通


//客户端事件部分
///////////////////////////////////////////////////////////////////////////////////////////////////////
//日志输出显示接口
function OnClientLogOutMessage(text) {

}


//客户端注册事件。
function OnClientLoginStatus(login_status, login_info) {
    //alert("a")
    //开启IE的调试模式.
    //debugger;
    // document.getElementById('theMessage').innerHTML = login_info

    if (login_status == 20000) {
    } else if (login_status == 21000) {
    } else if (login_status == 22000) {
    } else if (login_status == 23000) {
    } else if (login_status == 24000) {
    } else if (login_status == 25000) {
    }
}

//客户端呼入事件。
//注意：这里可能需要供VBS调用，VBScript 函数调用javascrip函数时  javascrip函数中的 ；必须全部去掉 不然调用不通
var income_chan_id = null;
function OnClientCallIncome(chan_id, call_from_id, call_subject) {
    //保存这里的通道ID，用于应答或者拒绝接听。
    income_chan_id = chan_id;
}

//客户端文本消息事件。
//注意：这里可能需要供VBS调用，VBScript 函数调用javascrip函数时  javascrip函数中的 ；必须全部去掉 不然调用不通
function OnClientPagerMessage(recv_from, msg_subject, msg_text) {
    //document.getElementById('theMessage').innerHTML = "收到短信来自：" + recv_from + "内容：" + msg_text ;
}

//客户端文本消息结果事件。
//注意：这里可能需要供VBS调用，VBScript 函数调用javascrip函数时  javascrip函数中的 ；必须全部去掉 不然调用不通
function OnPagerMessageResponse(request_send_to, message_id, response_code, response_info) {
}

